#include "esp_event.h"
#include <stdio.h>
#include <stdbool.h>
#include <unistd.h>

void app_main(void)
{
	nvs_flash_init();
	esp_netif_init();
	esp_event_loop_create_default();
	
    while (true) {
        printf("Hello from app_main!\n");
        sleep(1);
    }
}
